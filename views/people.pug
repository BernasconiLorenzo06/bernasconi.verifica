doctype html
html
  head
  body
    h1= persona.name.title,persona.name.first,persona.name.last
    p Email: #{persona.email}

    if persona.id && persona.id.value
      p ID: #{persona.id.value} 
    img(src=persona.picture.large)